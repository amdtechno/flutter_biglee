import 'package:siam_biglee/const/packages.dart';

class MyProgramScreens extends StatelessWidget {
  const MyProgramScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
